import React from 'react';

const UserMenu: React.FC = () => {
  return (
    <button className="p-2 rounded focus:outline-none focus:ring">
      User
    </button>
  );
};

export default UserMenu;
